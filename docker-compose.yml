version: "3.9"

services:

  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: remesas_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./BackEnd
    container_name: fastapi_backend
    depends_on:
      - db
    ports:
      - "8000:8000"
    volumes:
      - ./BackEnd:/app

volumes:
  postgres_data: